<div id="<%= dom_id post %>">
  <h2>
    <strong>Title:</strong>
    <%= post.title %>
  </h2>
  <h6><%= pluralize(post.views,'view') %></h6>

  <p>
    <strong>Body:</strong> 
    <%= post.body %>
  </p>

</div>
